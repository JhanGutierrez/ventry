import { AppPermission } from './app-permission';

describe('AppPermission', () => {
  it('should create an instance', () => {
    const directive = new AppPermission();
    expect(directive).toBeTruthy();
  });
});
