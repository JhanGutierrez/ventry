import { NestedCellValuePipe } from "./nested-cell-value-pipe";

describe('NestedCellValuePipe', () => {
  it('create an instance', () => {
    const pipe = new NestedCellValuePipe();
    expect(pipe).toBeTruthy();
  });
});
